name: Docker Image CI (argocd-cli)

on:
  push:
    branches: [ "master" ]
    paths:
      - 'argocd-cli/**'
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - name: Docker Login
      uses: docker/login-action@v3.0.0
      with:
        username: ${{ secrets.DOCKER_USER }}
        password: ${{ secrets.DOCKER_PASS }}
    - name: Build the Docker image
      working-directory: ./argocd-cli
      run: docker build . --file Dockerfile --tag ${{ secrets.DOCKER_USER }}/argocd-cli --push